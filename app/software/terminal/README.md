# 终端模拟器

## 功能介绍

终端模拟器用于在应用内执行常见系统命令，方便进行简单的目录查看、文件操作和命令调试。

## 使用方法

- 输入任意系统命令并回车执行。
- 输入 `exit` 或 `quit` 退出程序。
- 输入 `clear` 清除屏幕。
- 输入 `cd 目录路径` 切换工作目录。

## 注意事项

- 请谨慎执行删除、移动等会修改文件的命令。
- 命令执行权限受当前 Windows 用户权限限制。
